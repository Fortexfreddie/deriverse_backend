PORT=5000
# generic error masking in production, full stack traces in development
NODE_ENV=development # set to production in production
SOLANA_RPC_URL=https://api.devnet.solana.com or https://devnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY

# --- Deriverse Protocol Constants ---
PROGRAM_ID=Drvrseg8AQLP8B96DBGmHRjFGviFNYTkHueY9g3k27Gu
VERSION=14  # Updated from 14 to handle the buffer size change

# --- Database ---
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/deriverse_db?schema=public"

# Optional: Wallet + keypair
# WALLET_ADDRESS=YOUR_SOLANA_WALLET
# KEYPAIR_FILENAME=./keys/new_user.json

# --- AI / Generative Model Configuration ---
# Which provider to prefer for AI journaling: 'gemini' | 'deepseek' | 'openai' | 'mock'
AI_PROVIDER=gemini

# OpenAI-compatible SDK key (used as fallback for Gemini if present)
OPENAI_API_KEY=

# Google Gemini (via OpenAI-compatible wrapper) credentials
# If using Gemini set GEMINI_API_KEY (or use OPENAI_API_KEY if configured)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# DeepSeek credentials (OpenAI-compatible endpoint)
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# --- External Data APIs ---
# Get yours at: https://cryptopanic.com/developers/api/
CRYPTOPANIC_API_KEY=

# Get yours at: https://www.coingecko.com/en/api
COINGECKO_API_KEY=<your_key_here>

# Notes:
# - Fill API keys locally in a real .env (never commit secrets).
# - You can set AI_PROVIDER to 'mock' during local development to avoid external calls.